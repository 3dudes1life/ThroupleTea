THROUPLE TEA FINAL HOTLINE + GIVEAWAY FIX

Replace only the root script.js file in GitHub.

Changes:
- Header Throuple Hotline opens a pre-addressed email directly.
- Older episode/topic/archive Hotline links still become one-click: they arrive at the homepage hash and the email composer opens automatically.
- The existing weekly giveaway card is moved directly beneath the hero.
- The existing OneSignal button, IDs and event logic are preserved because the actual DOM card is moved, not copied.
- The lower Hotline section remains, now focused only on the email Hotline.
- No changes to index.html, OneSignal, service workers, manifest, sitemap, episode pages or episode-seo.css.

GitHub connector write access returned 403, so upload this file manually.
